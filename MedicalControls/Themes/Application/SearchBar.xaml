<ResourceDictionary
    x:Class="EPIC.MedicalControls.Themes.Application.SearchBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:entities="clr-namespace:EPIC.DataLayer.Entities;assembly=DataLayer"
    xmlns:general="clr-namespace:EPIC.MedicalControls.Themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reflection="clr-namespace:System.Reflection;assembly=mscorlib"
    xmlns:ribbon="clr-namespace:System.Windows.Controls.Ribbon;assembly=System.Windows.Controls.Ribbon"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    mc:Ignorable="d">
    <BitmapImage x:Key="Find" UriSource="/Resources/0025-search.png" />
    <ControlTemplate x:Key="SearchControlTemplate">
        <Grid Row="0">
            <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Label
                Grid.Column="0"
                VerticalAlignment="Center"
                Content="Search:" />
            <TextBox
                Name="SearchText"
                Grid.Row="1"
                Grid.Column="1"
                MinWidth="100"
                Margin="5"
                VerticalAlignment="Center"
                TextChanged="Search_TextChanged" />
            <Button
                Grid.RowSpan="2"
                Grid.Column="2"
                Margin="5"
                Padding="3"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Click="Button_Click">
                <StackPanel Orientation="Horizontal">
                    <Image
                        Width="20"
                        Height="20"
                        Margin="3,0"
                        Source="{StaticResource Find}" />
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">Find</TextBlock>
                </StackPanel>
            </Button>
        </Grid>
    </ControlTemplate>


    <Style x:Key="SearchControlStyle" TargetType="{x:Type ContentControl}">
        <Setter Property="Template" Value="{StaticResource SearchControlTemplate}" />
    </Style>

</ResourceDictionary>

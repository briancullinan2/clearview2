<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:classic="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
    xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Ribbon"
    xmlns:entities="clr-namespace:EPIC.DataLayer.Entities;assembly=DataLayer"
    xmlns:primitives="clr-namespace:System.Windows.Controls.Ribbon.Primitives;assembly=System.Windows.Controls.Ribbon"
    xmlns:ribbon="clr-namespace:System.Windows.Controls.Ribbon;assembly=System.Windows.Controls.Ribbon">
    <SolidColorBrush x:Key="RibbonDisabledForegroundBrushKey" Color="#FF9E9E9E" />
    <SolidColorBrush x:Key="RibbonDropDownBackgroundBrushKey" Color="#F0FFFFFF" />
    <SolidColorBrush x:Key="ResizeGripperBackgroundBrush" Color="#FFE5EFF9" />
    <SolidColorBrush x:Key="ResizeThumbPathBrush" Color="#FF6C747D" />
    <primitives:RibbonScrollButtonVisibilityConverter x:Key="RibbonScrollButtonVisibilityConverter" />

    <Style x:Key="AlertsRibbonGroupStyle" TargetType="{x:Type RibbonGroup}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ribbon:RibbonGroup">
                    <Border
                        Name="GroupBorder"
                        Margin="1,2,0,0"
                        VerticalAlignment="Stretch"
                        Background="{TemplateBinding Background}">
                        <Grid x:Name="MainGrid" VerticalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" MinHeight="16" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Border
                                Name="PART_HotBackground"
                                Grid.RowSpan="3"
                                Background="{TemplateBinding MouseOverBackground}"
                                BorderBrush="{TemplateBinding MouseOverBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="2"
                                Opacity="0"
                                SnapsToDevicePixels="True" />
                            <Border
                                Name="SeparatorBorder"
                                Grid.RowSpan="3"
                                Grid.Column="1"
                                Width="1"
                                Height="75"
                                VerticalAlignment="Center"
                                Background="{TemplateBinding BorderBrush}"
                                SnapsToDevicePixels="True" />
                            <Border Margin="2,1,2,0" Padding="3,0,3,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ContentControl Grid.Column="0" Content="{Binding Path=Items[0], RelativeSource={RelativeSource AncestorType={x:Type ribbon:RibbonGroup}}}" />
                                    <ContentControl Grid.Column="1" Content="{Binding Path=Items[1], RelativeSource={RelativeSource AncestorType={x:Type ribbon:RibbonGroup}}}" />
                                    <ContentControl Grid.Column="2" Content="{Binding Path=Items[2], RelativeSource={RelativeSource AncestorType={x:Type ribbon:RibbonGroup}}}" />
                                </Grid>
                            </Border>
                            <Border
                                Name="LabelSeparatorBorder"
                                Grid.Row="2"
                                Height="1"
                                Margin="5,0,1,15" />
                            <Grid
                                Grid.Row="2"
                                Grid.ColumnSpan="2"
                                Margin="2,0,2,0">
                                <ContentPresenter
                                    Name="PART_Header"
                                    MaxHeight="16"
                                    Margin="2,0,2,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Content="{Binding Path=Header, RelativeSource={RelativeSource AncestorType={x:Type ribbon:RibbonGroup}}}"
                                    TextElement.Foreground="{StaticResource RibbonDisabledForegroundBrushKey}" />
                            </Grid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

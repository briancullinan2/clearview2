<Window
	x:Class="EPIC.Controls.SelectEntity" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Title="SelectEntity"
	Height="300"
	Width="300"
	WindowStartupLocation="CenterOwner"
	ShowInTaskbar="False" xmlns:controls="clr-namespace:EPIC.Controls"
	Icon="{Binding Path=MainWindow.Icon, Source={x:Static Application.Current}}">
	<FrameworkElement.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary
					Source="/Themes/ClearView.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</FrameworkElement.Resources>
	<Grid
		Background="{x:Static SystemColors.ControlBrush}">
		<Grid.RowDefinitions>
			<RowDefinition
				Height="Auto" />
			<RowDefinition />
			<RowDefinition
				Height="Auto" />
		</Grid.RowDefinitions>
		<Label
			Content="Select an item from the list:" />
		<DataGrid
			Name="Entities"
			AutoGenerateColumns="False"
			Background="#FFFFFFFF"
			RowHeaderWidth="0"
			Margin="5"
			Grid.Row="1"
			IsReadOnly="True"
			SelectionUnit="FullRow"
			SelectionMode="Single"
			CanUserResizeRows="False">
			<DataGrid.RowHeaderStyle>
				<Style
					TargetType="{x:Type DataGridRowHeader}">
					<Setter
						Property="Visibility"
						Value="Collapsed" />
				</Style>
			</DataGrid.RowHeaderStyle>
			<DataGrid.CellStyle>
				<Style
					TargetType="{x:Type DataGridCell}">
					<Setter
						Property="BorderThickness"
						Value="0" />
				</Style>
			</DataGrid.CellStyle>
		</DataGrid>
		<StackPanel
			Grid.Row="2"
			Orientation="Horizontal"
			HorizontalAlignment="Right">
			<Button
				Content="Select"
				HorizontalAlignment="Right"
				Click="Select_Click" />
			<Button
				Content="Cancel"
				HorizontalAlignment="Right"
				Click="Cancel_Click" />
		</StackPanel>
	</Grid>
</Window>

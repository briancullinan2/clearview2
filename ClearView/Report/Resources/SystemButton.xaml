<Button
    x:Class="EPIC.Report.Resources.SystemButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:EPIC.Report.Resources"
    DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <StackPanel Orientation="Horizontal">
        <Image
            Width="93"
            Height="96"
            Margin="3,0"
            Stretch="None">
            <FrameworkElement.Style>
                <Style TargetType="{x:Type Image}">
                    <Setter Property="Source">
                        <Setter.Value>
                            <CroppedBitmap Source="{Binding Image}" SourceRect="0,96,93,96" />
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}" Value="True">
                            <Setter Property="Source">
                                <Setter.Value>
                                    <CroppedBitmap Source="{Binding Image}" SourceRect="0,0,93,96" />
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </FrameworkElement.Style>
        </Image>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">Sensory &amp; Skeletal Systems</TextBlock>
    </StackPanel>
</Button>

<Button
	x:Class="EPIC.Report.Resources.SystemButton" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:resources="clr-namespace:EPIC.Report.Resources"
	DataContext="{Binding RelativeSource={RelativeSource Self}}">
	<StackPanel
		Orientation="Horizontal">
		<Image
			Stretch="None"
			Height="96"
			Width="93"
			Margin="3,0">
			<FrameworkElement.Style>
				<Style
					TargetType="{x:Type Image}">
					<Setter
						Property="Source">
						<Setter.Value>
							<CroppedBitmap
								SourceRect="0,96,93,96"
								Source="{Binding Image}" />
						</Setter.Value>
					</Setter>
					<Style.Triggers>
						<DataTrigger
							Value="True"
							Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
							<Setter
								Property="Source">
								<Setter.Value>
									<CroppedBitmap
										SourceRect="0,0,93,96"
										Source="{Binding Image}" />
								</Setter.Value>
							</Setter>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</FrameworkElement.Style>
		</Image>
		<TextBlock
			VerticalAlignment="Center"
			HorizontalAlignment="Center">Sensory &amp; Skeletal Systems</TextBlock>
	</StackPanel>
</Button>

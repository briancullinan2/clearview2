<ResourceDictionary
    x:Class="EPIC.ClearView.Themes.User.Search"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:application="clr-namespace:EPIC.ClearView.Pages.Application"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ribbon="clr-namespace:System.Windows.Controls.Ribbon;assembly=System.Windows.Controls.Ribbon"
    xmlns:user="clr-namespace:EPIC.ClearView.Pages.User">
    <BitmapImage x:Key="Find" UriSource="/Resources/0025-search.png" />
    <BitmapImage x:Key="NewUser" UriSource="/Resources/4505-user_add.png" />
    <BitmapImage x:Key="ViewUser" UriSource="/Resources/4504-user_search.png" />
    <BitmapImage x:Key="SearchUser" UriSource="/Resources/4504-user_search.png" />
    <BitmapImage x:Key="Permissions" UriSource="/Resources/0080-doc_ok.png" />
    <x:Array x:Key="SearchOptions" Type="{x:Type ribbon:RibbonGroup}">
        <ribbon:RibbonGroup Header="User Options">
            <ribbon:RibbonToggleButton
                Command="{DynamicResource CloseTabCommand}"
                IsChecked="True"
                Label="Search"
                LargeImageSource="{StaticResource SearchUser}" />
        </ribbon:RibbonGroup>
        <ribbon:RibbonGroup Header="User Options">
            <ribbon:RibbonButton
                Command="{DynamicResource ShowTabCommand}"
                CommandParameter="{x:Type user:Add}"
                Label="New User"
                LargeImageSource="{StaticResource NewUser}" />
            <ribbon:RibbonButton
                Name="UserPermission"
                Command="{DynamicResource ShowTabCommand}"
                CommandParameter="{x:Type application:Permissions}"
                Label="Permissions"
                LargeImageSource="{StaticResource Permissions}" />
        </ribbon:RibbonGroup>
    </x:Array>
</ResourceDictionary>

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/EPICCLearView;component/Themes/TextBoxes/TextBoxesBase.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Style
        x:Key="{x:Type PasswordBox}"
        BasedOn="{StaticResource TextBoxesBaseStyle}"
        TargetType="{x:Type PasswordBox}">
        <Setter Property="PasswordBox.PasswordChar" Value="●" />
        <Setter Property="PasswordBox.CaretBrush" Value="{StaticResource ForegroundBrush}" />
        <Setter Property="PasswordBox.SelectionBrush" Value="{StaticResource MiddleLightBrush}" />
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True" />
        <Style.Triggers>
            <Trigger Property="UIElement.IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
